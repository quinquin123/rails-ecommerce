<div class="seller-report-container">
  <div class="report-header">
    <h1>Seller Dashboard</h1>
    <p class="report-date">Report generated on <%= Date.current.strftime('%B %d, %Y') %></p>
  </div>

  <!-- Key Metrics Summary -->
  <div class="metrics-grid">
    <div class="metric-card primary">
      <h3>Total Earnings</h3>
      <div class="metric-value"><%= number_to_currency(@total_earnings) %></div>
    </div>
    
    <div class="metric-card">
      <h3>Total Sales</h3>
      <div class="metric-value"><%= @sales_count %></div>
    </div>
    
    <div class="metric-card">
      <h3>Average Order Value</h3>
      <div class="metric-value"><%= number_to_currency(@average_order_value) %></div>
    </div>
    
    <div class="metric-card">
      <h3>Pending Orders</h3>
      <div class="metric-value"><%= @pending_orders %></div>
    </div>
  </div>

  <!-- Performance Metrics -->
  <div class="performance-section">
    <h2>Performance Overview</h2>
    <div class="performance-grid">
      <div class="performance-item">
        <span class="label">Average Product Rating:</span>
        <span class="value"><%= number_with_precision(@average_rating, precision: 1) %> ⭐</span>
      </div>
      <div class="performance-item">
        <span class="label">Return Rate:</span>
        <span class="value"><%= @return_rate %>%</span>
      </div>
      <div class="performance-item">
        <span class="label">Repeat Customers:</span>
        <span class="value"><%= @repeat_customers_count %></span>
      </div>
    </div>
  </div>

  <!-- Charts Section -->
  <div class="charts-section">
    <div class="chart-container">
      <h3>Monthly Earnings Trend</h3>
      <div class="chart-placeholder">
        <!-- You can integrate Chart.js or similar here -->
        <% @monthly_earnings.each do |month, earnings| %>
          <div class="chart-bar">
            <span class="month"><%= month %></span>
            <span class="earnings"><%= number_to_currency(earnings) %></span>
          </div>
        <% end %>
      </div>
    </div>

    <div class="chart-container">
      <h3>Weekly Sales Volume</h3>
      <div class="chart-placeholder">
        <% @weekly_sales.each do |week, count| %>
          <div class="chart-bar">
            <span class="week"><%= week %></span>
            <span class="count"><%= count %> sales</span>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <!-- Product Analytics -->
  <div class="products-section">
    <div class="product-analytics-grid">
      <!-- Top Products by Revenue -->
      <div class="product-list-container">
        <h3>🏆 Top 5 Products by Revenue</h3>
        <% if @top_products_by_revenue.any? %>
          <ul class="product-list">
            <% @top_products_by_revenue.each_with_index do |(product, revenue), index| %>
              <li class="product-item">
                <span class="rank">#<%= index + 1 %></span>
                <%= link_to product.title, product_path(product), class: 'product-link' %>
                <span class="metric"><%= number_to_currency(revenue) %></span>
              </li>
            <% end %>
          </ul>
        <% else %>
          <p class="no-data">No sales data available yet.</p>
        <% end %>
      </div>

      <!-- Top Products by Sales Volume -->
      <div class="product-list-container">
        <h3>📦 Top 5 Products by Sales Volume</h3>
        <% if @top_products_by_sales.any? %>
          <ul class="product-list">
            <% @top_products_by_sales.each_with_index do |(product, quantity), index| %>
              <li class="product-item">
                <span class="rank">#<%= index + 1 %></span>
                <%= link_to product.title, product_path(product), class: 'product-link' %>
                <span class="metric"><%= quantity %> sold</span>
              </li>
            <% end %>
          </ul>
        <% else %>
          <p class="no-data">No sales data available yet.</p>
        <% end %>
      </div>

      <!-- Top Products by Reviews -->
      <div class="product-list-container">
        <h3>⭐ Top 5 Products by Reviews</h3>
        <% if @top_products_by_reviews.any? %>
          <ul class="product-list">
            <% @top_products_by_reviews.each_with_index do |product, index| %>
              <li class="product-item">
                <span class="rank">#<%= index + 1 %></span>
                <%= link_to product.title, product_path(product), class: 'product-link' %>
                <span class="metric"><%= product.reviews_count %> reviews</span>
              </li>
            <% end %>
          </ul>
        <% else %>
          <p class="no-data">No products with reviews yet.</p>
        <% end %>
      </div>
    </div>
  </div>

  <!-- Inventory Management -->
  <div class="inventory-section">
    <h2>Product Overview</h2>
    <div class="inventory-grid">
      <div class="inventory-card">
        <h4>Total Products</h4>
        <div class="inventory-value"><%= @total_products %></div>
      </div>
      <div class="inventory-card">
        <h4>Active Products</h4>
        <div class="inventory-value"><%= @active_products %></div>
      </div>
      <div class="inventory-card">
        <h4>Moderated Products</h4>
        <div class="inventory-value warning"><%= @moderated_products %></div>
      </div>
      <div class="inventory-card">
        <h4>Deleted Products</h4>
        <div class="inventory-value"><%= @deleted_products %></div>
      </div>
    </div>
  </div>
</div>

<style>
  .seller-report-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 24px;
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
    min-height: 100vh;
  }

  .report-header {
    text-align: center;
    margin-bottom: 40px;
    padding: 32px 0;
    background: white;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.08);
    position: relative;
    overflow: hidden;
  }

  .report-header::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
  }

  .report-header h1 {
    color: #1a202c;
    margin-bottom: 8px;
    font-size: 2.5rem;
    font-weight: 700;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .report-date {
    color: #64748b;
    font-size: 1.1rem;
    font-weight: 500;
  }

  .metrics-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 24px;
    margin-bottom: 40px;
  }

  .metric-card {
    background: white;
    border-radius: 16px;
    padding: 28px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.08);
    border: 1px solid rgba(255,255,255,0.2);
    position: relative;
    transition: all 0.3s ease;
    overflow: hidden;
  }

  .metric-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, #e2e8f0 0%, #cbd5e0 100%);
  }

  .metric-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 30px rgba(0,0,0,0.12);
  }

  .metric-card.primary {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
  }

  .metric-card.primary::before {
    background: linear-gradient(90deg, rgba(255,255,255,0.3) 0%, rgba(255,255,255,0.1) 100%);
  }

  .metric-card h3 {
    margin: 0 0 16px 0;
    font-size: 0.875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    opacity: 0.8;
  }

  .metric-value {
    font-size: 2.25rem;
    font-weight: 800;
    line-height: 1;
  }

  .performance-section, .products-section, .inventory-section, .customers-section, .action-items {
    margin-bottom: 48px;
  }

  .performance-section h2, .products-section h2, .inventory-section h2 {
    font-size: 1.75rem;
    font-weight: 700;
    color: #1a202c;
    margin-bottom: 24px;
    padding-left: 16px;
    border-left: 4px solid #667eea;
  }

  .performance-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 24px;
    background: white;
    padding: 32px;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.08);
  }

  .performance-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 16px 0;
    border-bottom: 1px solid #f1f5f9;
    transition: all 0.2s ease;
  }

  .performance-item:hover {
    padding-left: 8px;
    background: linear-gradient(90deg, rgba(102,126,234,0.05) 0%, transparent 100%);
  }

  .performance-item:last-child {
    border-bottom: none;
  }

  .performance-item .label {
    font-weight: 600;
    color: #374151;
  }

  .performance-item .value {
    font-weight: 700;
    color: #667eea;
    font-size: 1.1rem;
  }

  .charts-section {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    margin-bottom: 40px;
  }

  .chart-container {
    background: white;
    border-radius: 16px;
    padding: 28px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.08);
    transition: transform 0.3s ease;
  }

  .chart-container:hover {
    transform: translateY(-2px);
  }

  .chart-container h3 {
    margin: 0 0 24px 0;
    font-size: 1.25rem;
    font-weight: 600;
    color: #1a202c;
  }

  .chart-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 12px 16px;
    margin: 8px 0;
    background: linear-gradient(90deg, #f8fafc 0%, #f1f5f9 100%);
    border-radius: 8px;
    border-left: 3px solid #667eea;
    transition: all 0.2s ease;
  }

  .chart-bar:hover {
    background: linear-gradient(90deg, #e2e8f0 0%, #cbd5e0 100%);
    transform: translateX(4px);
  }

  .product-analytics-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 24px;
  }

  .product-list-container {
    background: white;
    border-radius: 16px;
    padding: 28px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.08);
    transition: transform 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .product-list-container::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, #f093fb 0%, #f5576c 50%, #4facfe 100%);
  }

  .product-list-container:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 30px rgba(0,0,0,0.12);
  }

  .product-list-container h3 {
    margin: 0 0 24px 0;
    font-size: 1.25rem;
    font-weight: 700;
    color: #1a202c;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .product-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .product-item {
    display: flex;
    align-items: center;
    padding: 16px 0;
    border-bottom: 1px solid #f1f5f9;
    transition: all 0.2s ease;
  }

  .product-item:hover {
    background: linear-gradient(90deg, rgba(102,126,234,0.05) 0%, transparent 100%);
    padding-left: 8px;
    border-radius: 8px;
  }

  .product-item:last-child {
    border-bottom: none;
  }

  .rank {
    font-weight: 800;
    background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-right: 16px;
    min-width: 32px;
    font-size: 1.1rem;
  }

  .product-link {
    flex: 1;
    text-decoration: none;
    color: #374151;
    font-weight: 500;
    transition: color 0.2s ease;
  }

  .product-link:hover {
    color: #667eea;
    text-decoration: none;
  }

  .metric {
    font-weight: 700;
    color: #667eea;
    background: linear-gradient(135deg, #e0e7ff 0%, #c7d2fe 100%);
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 0.875rem;
  }

  .inventory-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 24px;
    margin-bottom: 24px;
  }

  .inventory-card {
    background: white;
    border-radius: 16px;
    padding: 28px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.08);
    text-align: center;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .inventory-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, #4facfe 0%, #00f2fe 100%);
  }

  .inventory-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 30px rgba(0,0,0,0.12);
  }

  .inventory-card h4 {
    margin: 0 0 16px 0;
    color: #64748b;
    font-size: 0.875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .inventory-value {
    font-size: 2rem;
    font-weight: 800;
    color: #1a202c;
  }

  .inventory-value.warning {
    color: #ef4444;
  }

  .no-data {
    color: #94a3b8;
    font-style: italic;
    text-align: center;
    padding: 40px 20px;
    background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
    border-radius: 12px;
    border: 2px dashed #cbd5e0;
  }

  @media (max-width: 768px) {
    .seller-report-container {
      padding: 16px;
    }
    
    .charts-section {
      grid-template-columns: 1fr;
    }
    
    .metrics-grid {
      grid-template-columns: 1fr;
    }

    .product-analytics-grid {
      grid-template-columns: 1fr;
    }

    .report-header h1 {
      font-size: 2rem;
    }

    .metric-value {
      font-size: 1.875rem;
    }
  }

  /* Animation for loading */
  .seller-report-container * {
    animation: slideInUp 0.6s ease-out;
  }

  @keyframes slideInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .metrics-grid .metric-card:nth-child(1) { animation-delay: 0.1s; }
  .metrics-grid .metric-card:nth-child(2) { animation-delay: 0.2s; }
  .metrics-grid .metric-card:nth-child(3) { animation-delay: 0.3s; }
  .metrics-grid .metric-card:nth-child(4) { animation-delay: 0.4s; }
</style>